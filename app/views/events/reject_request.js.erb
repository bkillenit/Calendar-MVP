// params id because params to the event controller for the accept action
$('<%= "#event-#{params[:id]}" %>').removeClass('unconfirmed-event');
$('<%= "#event-#{params[:id]}" %>').addClass('hide');

//destroys popover next to the event
$('<%= "#event-#{params[:id]}" %>').popover('destroy');