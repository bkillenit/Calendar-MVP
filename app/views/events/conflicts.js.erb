# basic idea: return seg1.end > seg2.start && seg1.start < seg2.end;
# render User.find(event.user_id) for each event overlapping the position of
# the users mouse

<% if session[:merged_users] && params[:hover_date] %>
  <% @conflicts = Event.find_conflicts(params[:hover_date], session[:merged_users]) %>
<% end %>
