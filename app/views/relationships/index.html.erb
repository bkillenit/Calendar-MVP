<h1>Listing relationships</h1>

<table>
    <tr>
        <th>Name</th>
        <th>follower id</th>
        <th>followed id</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

    <% @users.each do |user| %>
    <tr>
        <td><%= user.name %></td>
        <td><%= user.follower_id %></td>
        <td><%= user.followed_id %></td>
        <td><%= user.id %></td>
    </tr>
    <% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %> <br/>
<%= link_to 'Show Calendar', calendar_index_path %>