// this code right now is just a transplant of the code from the jquery-extras
// merge_user function
/* var user_id_to_merge = "<%= params[:id] %>"
var source = "/users/" + user_id_to_merge + "/events";
           $('#calendar').fullCalendar('addEventSource', {
               url: source,
             className: 'merged-event ' + user_id_to_merge,
               editable: false
             });
$('#calendar').fullCalendar('rerenderEvents');
alert("user merged!"); */

